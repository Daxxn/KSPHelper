<Window x:Class="KSPHelperWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KSPHelperWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="File" />
            <MenuItem Header="Run Part Reader" Name="GameDataReaderButton" ToolTip="This may take a while..." />
        </Menu>

        <TabControl Grid.Row="1">
            <TabItem
                x:Name="ElectricalTab"
                Header="Electrical"
                IsSelected="True"
            />
            <TabItem x:Name="CommsTab" Header="Comms" />
        </TabControl>
    </Grid>
</Window>
